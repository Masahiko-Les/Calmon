/**
 * カルモン 定数・閾値
 * ================================
 * ゲームバランスに関わるすべての数値をここで管理します。
 * 調整したいときはこのファイルだけ変更すれば OK です。
 */

import type { CalmonState } from '../types';

// ------------------------------------------------
// 空腹ゲージ設定
// ------------------------------------------------

export const HUNGER = {
  /** ゲージの最大値 */
  MAX: 100,
  /** アプリ初回起動時の初期値 */
  INITIAL: 80,
  /** 1時間あたりの減少量 */
  DECAY_PER_HOUR: 5,
} as const;

// ------------------------------------------------
// 状態遷移の閾値
// ------------------------------------------------

/**
 * 空腹ゲージがこの値以上のとき、対応する状態になります。
 *
 * ゲージ値 → 状態
 *   65 以上   → normal
 *   30 〜 64  → hungry
 *    1 〜 29  → weak
 *    0 & 一定時間経過 → ghost  (それ以外は weak のまま)
 */
export const STATE_THRESHOLDS: Record<CalmonState, number> = {
  normal: 65,
  hungry: 30,
  weak: 1,
  ghost: 0,
};

/** 空腹ゲージが 0 のまま何時間経過したら ghost になるか */
export const GHOST_TIME_HOURS = 2;

/** ghost 状態でもエサをあげると復活できるか (false にすると復活不可) */
export const CAN_REVIVE_FROM_GHOST = true;

// ------------------------------------------------
// エサ設定
// ------------------------------------------------

/** 初回起動時のエサ在庫 */
export const INITIAL_FEED_STOCK = 50;

/** 1kcal あたりに変換されるエサポイント */
export const CALORIE_TO_FEED_RATE = 1;

/** エサ 1 ポイントあたりの空腹ゲージ回復量 */
export const HUNGER_PER_FEED = 1;

/** 「エサをあげる」ボタンの選択肢 (エサポイント) */
export const FEED_OPTIONS = [10, 50] as const;

// ------------------------------------------------
// リアクションメッセージ
// ------------------------------------------------

/**
 * エサをあげたときに表示するメッセージ。
 * キャラクターの状態ごとにランダムで選ばれます。
 */
export const REACTION_MESSAGES: Record<CalmonState, string[]> = {
  normal: ['うれしそう！', 'もぐもぐ食べてる！'],
  hungry: ['助かったみたい！', 'ちょっと元気になった！'],
  weak: ['ふらふらしながら食べている…', '少し回復した！'],
  ghost: ['魂が少し戻ってきた…', 'カルモンが反応した！'],
};

// ------------------------------------------------
// UI カラー (状態別)
// ------------------------------------------------

/** 状態ごとのテーマカラー (ゲージ・バッジ等に使用) */
export const STATE_COLORS: Record<CalmonState, string> = {
  normal: '#7FC8F8',  // 元気 → 明るい青
  hungry: '#F6B26B',  // 空腹 → オレンジ
  weak: '#E06060',    // 衰弱 → 赤
  ghost: '#B0A8C8',   // 幽霊 → 薄紫
};

/** 状態ごとの日本語ラベル */
export const STATE_LABELS: Record<CalmonState, string> = {
  normal: 'げんき',
  hungry: 'おなかすいた',
  weak: 'よわよわ…',
  ghost: 'おばけ…',
};
